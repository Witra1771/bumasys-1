{{--<img src="{{ asset('flags/'. $code .'.png') }}" alt="{{ $name }}" class="{{ config('language.flags.img_class') }}" width="{{ config('language.flags.width') }}" />--}}

<img src="{{ asset('flags/'. $code .'.png') }}" alt="{{ $name }}" class="me-2 rounded" width="20"  height="20">
<span class="align-middle">{{ $name }}</span>
